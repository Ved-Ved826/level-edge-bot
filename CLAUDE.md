# LevelEdge — Discord-бот учёта активности нового поколения

## Язык общения
Всегда отвечай и объясняй свои действия по-русски, независимо от того, на каком языке написаны названия переменных, комментарии в коде или логи — только чистый и понятный русский язык.

## Что это
Ультрабыстрый, стильный серверный трекер активности, уровней и соревновательных механик:
1. **Edge-архитектура Cloudflare Workers** для мгновенных ответов на слэш-команды (<300 мс) без задержек и очередей.
2. **Дизайнерские PNG-карточки** через Satori + WASM (тёмная тема Discord, аватарка, статус-кольцо, неоновый градиент, расчёт оставшегося XP).
3. **0 рублей затрат** — бесплатные лимиты Cloudflare Workers, Turso (libSQL) и контейнер на Render.
4. **Геймификация:** ежедневные квесты, активность за день (`/rank-today`), стрики 🔥, дуэли на опыт (`/duel`), войс-дропы 🎁, кастомизация карточки, скрытые ачивки, сезоны и ежегодный «Server King» (Discord Wrapped).

---

## Архитектура системы

### 1. Collector Daemon (`/collector`, Node.js в Docker на Render)
- Держит постоянный WebSocket Gateway с Discord.
- Отвечает за непрерывный сбор данных: антиспам-кулдаун на сообщения (30 сек), сегментный учёт войса (без накрутки при заглушении `selfDeaf + selfMute`).
- Ведёт посуточный учёт активности (`user_daily_activity`), прогресс дейликов, стрики и триггеры ивентов в войсе.
- Имеет встроенный HTTP Healthcheck-сервер на порту 10000 для Render.
- **НЕ** рендерит тяжелые картинки и **НЕ** отвечает на слэш-команды.

### 2. Cloudflare Worker (`/worker`, TypeScript Edge Worker)
- Принимает HTTP Interactions Endpoint от Discord (Ed25519 верификация подписи).
- Моментальный ответ `type: 5` (DEFERRED) + асинхронный фоновый рендер через `ctx.waitUntil(...)`.
- Рендерит карточки через Satori (JSX → SVG) и `@resvg/resvg-wasm` (SVG → PNG) с кэшированием `wasmInitPromise`.
- Отвечает на слэш-команды (`/rank`, `/rank-today`, `/leaderboard`, `/quests`, `/duel` и др.) и обрабатывает клики по кнопкам (`type: 7` UPDATE_MESSAGE или прямое обновление).

### 3. Serverless DB (Turso / libSQL)
- Единая база данных, доступная по HTTP API обоим сервисам.

---

## ⚠️ СТАТУС ВЫПОЛНЕНИЯ (ВАЖНО ДЛЯ МОДЕЛИ)

- ✅ **Этап 0a (Схема БД и структура):** ВЫПОЛНЕНО.
- ✅ **Этап 0b (Collector daemon):** ВЫПОЛНЕНО. Работает на Render в Docker, автоматически мигрирует колонки, собирает сообщения и войс.
- ✅ **Этап 0c (Worker /rank):** ВЫПОЛНЕНО. Рендерит карточку через Satori + WASM с поддержкой шрифта Roboto (Cyrillic + Latin) и SVG-иконками. **НЕ ТРОГАТЬ Card.tsx БЕЗ ЯВНОЙ ПРОСЬБЫ.**
- ✅ **Этап 0d (Worker /leaderboard):** ВЫПОЛНЕНО. Работает с кнопками пагинации (◀ Назад / Вперед ▶) через `type: 7`.
- ✅ **Этап 1 (Ежедневные квесты и активность за сегодня):** ВЫПОЛНЕНО. Работают `/rank-today`, `/quests` с авто-генерацией дня и трекингом в collector.
- 🔄 **ТЕКУЩИЙ ЭТАП — Этап 2 (Стрики активности и огоньки 🔥).**

---

## ДОРОЖНАЯ КАРТА ЭТАПОВ

### Этап 2 — Стрики активности (Activity Streaks / Огоньки 🔥)
- **База данных:**
  - Колонки в `users`: `streak_days INTEGER DEFAULT 0`, `last_streak_date TEXT DEFAULT NULL`, `streak_freezes INTEGER DEFAULT 0`.
- **Логика удержания (FOMO):**
  - Если пользователь выполнил за день норму (например, закрыл хотя бы 1 дейлик ИЛИ набрал 20 сообщений / 15 мин войса):
    - Если `last_streak_date` равен вчерашнему дню: `streak_days += 1`, `last_streak_date = today`.
    - Если сегодня уже обновлялся: ничего не меняем.
    - Если прошло 2+ дня: проверяем `streak_freezes > 0`. Если есть заморозка — тратим её (`streak_freezes -= 1`) и сохраняем стрик. Если нет — сброс `streak_days = 1`.
- **Множитель опыта:**
  - Стрик 3+ дня: +5% к получаемому XP.
  - Стрик 7+ дней: +10% к XP.
  - Стрик 14+ дней: +15% к XP.
  - Стрик 30+ дней: +25% к XP.
- **Отображение:**
  - На карточке `/rank`: яркий бейдж `🔥 N дней` рядом с уровнем.
  - В `/rank-today`: индикатор состояния стрика и наличие заморозок.

---

### Этап 3 — Дуэли на опыт (`/duel`) с серверным налогом 26%
- **Концепция:** азартные PVP-схватки участников 1 на 1 на ставку опыта.
- **Команда:** `/duel opponent:@User bet:XP` (мин. 50 XP, макс. 1000 XP или лимит от баланса).
  - Проверка: у обоих участников должно быть достаточно XP.
  - Нельзя дуэлиться с ботами или с самим собой.
- **Интерактивное приглашение (Discord Buttons):**
  - Бот отправляет сообщение: `⚔️ <@user1> бросил вызов <@user2> на ставку {bet} XP! [Принять] [Отклонить]`.
  - Таймаут на принятие: 60 секунд.
- **Механика дуэли:**
  - После нажатия [Принять]: мини-игра в кубики (Dice Roll) или гонка реакции (кто первый нажмёт на зелёную кнопку из сетки).
- **Экономика и налог 26% (Анти-перелив):**
  - Общий банк: `total_pot = bet * 2`.
  - Налог сервера строго **26%** с округлением до целого:
    `tax = Math.round(total_pot * 0.26)`
  - Чистый выигрыш победителя:
    `payout = total_pot - tax`
  - Победитель получает `payout - bet` чистого профита, проигравший теряет `bet`.
  - Сожжённый налог списывается из экономики сервера (сжигает XP от накруток).

---

### Этап 4 — Войс-дропы (Air Drops 🎁) и Счастливые часы (Happy Hours)
- **Войс-дропы (Лутбоксы):**
  - Если в голосовом канале находятся от 3+ реальных участников без мута более 20 минут:
  - С вероятностью раз в 30–45 минут в текстовый чат голосового канала падает ивент:
    `🎁 С неба упал контейнер с припасами! Нажми кнопку первым, чтобы забрать лут!`
  - Кнопка: `[ Забрать дроп! ]`.
  - Первый кликнувший получает рандомную награду: от 50 до 200 XP или временный билет «Заморозка стрика».
- **Счастливые часы (Happy Hours):**
  - Коллектор раз в день случайно выбирает интервал в 1 час (например, 19:00–20:00 или 21:00–22:00 UTC).
  - Оповещение в системный канал: `⚡ СЧАСТЛИВЫЙ ЧАС НАЧАЛСЯ! Двойной XP (2X) за все сообщения и войс на ближайшие 60 минут!`
  - Умножение начисляемого XP в `collector.ts` ровно в 2 раза.

---

### Этап 5 — Кастомизация карточки `/rank` (Темы, Неон, Титулы)
- **Таблица `user_cosmetics`:**
  `user_id TEXT, guild_id TEXT, theme_id TEXT DEFAULT 'default', title_id TEXT DEFAULT NULL, badges TEXT DEFAULT '[]', PRIMARY KEY(user_id, guild_id)`.
- **Команда `/card-customize`:**
  - Меню выбора темы оформления (Discord Select Menu):
    - `default` — классический тёмный Discord (Blurple + Green).
    - `cyberpunk` — неоновый розовый + бирюзовый градиент.
    - `magma` — вулканический тёмно-красный + оранжевый огонь.
    - `midnight` — глубокий индиго + фиолетовый неон.
    - `emerald` — ядовито-зелёный + золотой акцент.
- **Титулы под ником на карточке:**
  - Короткие звания: `[Гроза Войса]`, `[Ночная Сова]`, `[Снайпер Чата]`, `[Лудоман]`.
- **Рендеринг в `Card.tsx`:**
  - Динамическая подстановка палитры градиентов и свечения `boxShadow` в зависимости от `theme_id`.

---

### Этап 6 — Скрытые ачивки и пасхалки (Easter Eggs)
- **Таблица `user_achievements`:**
  `user_id TEXT, guild_id TEXT, achievement_id TEXT, unlocked_at INTEGER, PRIMARY KEY(user_id, guild_id, achievement_id)`.
- **Секретные условия (нет открытого списка, игроки ищут сами):**
  - 🤫 *«Тихий сыч»* — просидеть 2 часа в войсе с активными говорящими, не издав звука.
  - 🕛 *«Полуночник»* — отправить сообщение ровно в 00:00 UTC.
  - 🎰 *«Три топора»* — зафиксировать ровно 777 XP на балансе.
  - 🎲 *«Азартный безумец»* — выиграть 3 дуэли подряд.
  - 🧊 *«На волоске»* — спасти 14-дневный стрик с помощью заморозки.
- **Эффект открытия:**
  - В общий чат падает красивый золотой Embed: `🎉 СЕКРЕТНОЕ ДОСТИЖЕНИЕ РАЗБЛОКИРОВАНО! <@user> открыл «...» (+250 XP)!`.
  - Значок ачивки навсегда добавляется в профиль карточки `/rank`.

---

### Этап 7 — Еженедельные лидеры и Сезонные Лиги (Вайпы 4 раза в год)
- **Еженедельный спринт («Чемпион недели»):**
  - Таблица `weekly_activity` (сброс в ночь на понедельник 00:00 UTC).
  - Выдача временной роли «Чемпион недели» топ-1 сервера + бонусный несгораемый XP.
- **4 Сезона в год:**
  - 🌸 *Весенний кубок* (1 марта — 31 мая)
  - ☀️ *Летний драйв* (1 июня — 31 августа)
  - 🍂 *Осенний марафон* (1 сентября — 30 ноября)
  - ❄️ *Зимняя битва* (1 декабря — 28/29 февраля)
- **Механика вайпа:**
  - `total_xp` остаётся навсегда (вечный уровень аккаунта).
  - `season_xp` сбрасывается в 0 в конце каждого сезона с записью топа в `season_archive`.
  - В `/leaderboard` кнопки переключения: `[Текущий сезон]` и `[За всё время]`.

---

### Этап 8 — Годовые номинации «[Server_Name] KING / RECAP» (Spotify Wrapped)
- Ежегодное торжественное подведение итогов 31 декабря в 18:00 UTC.
- Номинации:
  - 👑 **Chat King** — абсолютный рекордсмен по сообщениям за год.
  - 🎙 **Voice King** — больше всего часов в голосовых комнатах.
  - 🦉 **Night Owl** — ночная активность с 01:00 до 06:00.
  - ⚡ **Iron Butt** — самая долгая непрерывная войс-сессия года.
  - 🎯 **Quest Master** — рекорд по закрытым дейликам.
- Команда `/recap` — персональная карточка года игрока (часы, любимый канал, лучший друг по войсу).

---

### Этап 9 — Система престижа (Prestige & Stars)
- Максимальный уровень `MAX_LEVEL = 100`.
- При достижении 100 уровня на карточке `/rank` открывается кнопка **«Престиж»**.
- Сброс уровня в 0 с начислением несгораемой звезды: `prestige_count += 1`.
- Отображение звёзд на карточке `Card.tsx` (например, `★ x2`).

---

### Этап 10 — CSV-экспорт для владельцев и администраторов
- Команда `/export` (только для прав `ADMINISTRATOR`).
- Выгрузка полной аналитики сервера: `user_id`, никнейм, `total_xp`, `season_xp`, `messages_count`, `voice_seconds`, `streak_days`, `prestige_count`.
- Отправка файла прямо в чат с защитой от спама (кулдаун 10 минут).

---

## ⚠️ КРИТИЧЕСКИЕ ПРАВИЛА ДЛЯ ОКРУЖЕНИЯ WINDOWS И ПРОЦЕССОВ

### 1. БЕЗОПАСНОСТЬ ЛОКАЛЬНОГО ШЛЮЗА OMNIROUTE (ПОРТ 20128)
- **КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО** выполнять массовое убийство процессов: `taskkill /F /IM node.exe`, `killall node` и аналоги!
- На машине запущен локальный AI-шлюз **Omniroute** на порту `20128` (процесс Node.js), через который идут все запросы модели. Убийство `node.exe` моментально уничтожает API-соединение с ошибкой `ConnectionRefused`.
- Для остановки зависших процессов разработки (например, wrangler на 8787):
  1. Найти точный PID: `netstat -ano | findstr :8787`
  2. Завершить ТОЛЬКО этот PID: `taskkill /PID <PID> /F`

### 2. РАБОТА С ФАЙЛАМИ В WINDOWS
- **НИКОГДА** не используй незакавыченный `cat << EOF` в bash/sh оболочке: конструкции `${var}` воспринимаются как переменные bash и стираются, ломая TypeScript/JSX код.
- При полной перезаписи файлов следи за тем, чтобы инструмент не склеивал старый и новый код (проверяй количество строк через `wc -l` или `git diff`).
- Чистая безопасная запись при необходимости:
  `node -e "fs.writeFileSync('путь', Buffer.from('...', 'utf-8'))"`

### 3. ПРЕДОТВРАЩЕНИЕ БЕСКОНЕЧНЫХ ЦИКЛОВ ПРАВОК
- Если после компиляции линтер сообщает об ошибках: **ОСТАНОВИСЬ**. Проверь целостность файла, а не запускай 10 слепых микро-редактирований подряд.