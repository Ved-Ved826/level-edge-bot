Ты автономный senior-разработчик. Твоя задача — довести до идеала и рабочего состояния функционал /rank-today и подсчёт активности, а затем всё собрать и проверить без участия человека.

1. Cloudflare Worker (воркер):
   - Проверь worker/wrangler.jsonc: посмотри, какой файл указан в поле "main" (например, src/worker.ts или src/index.ts).
   - Проверь этот main-файл. Обрабатывает ли он команду /rank-today сам напрямую или делегирует в worker/src/commands/rank.ts?
   - Убедись, что везде, где формируется ответ для /rank-today (в worker/src/commands/rank.ts и в worker/src/worker.ts, если там есть дублирующий код), время в войсе форматируется с секундами:
     const voicesec = activity.voice_seconds;
     const vh = Math.floor(voicesec / 3600);
     const vm = Math.floor((voicesec % 3600) / 60);
     const vs = voicesec % 60;
     Поле: vh > 0 ? `**${vh} ч. ${vm} мин. ${vs} сек.**` : `**${vm} мин. ${vs} сек.**`
   - Если в worker.ts логика дублируется — унифицируй её или подключи вызов из commands/rank.ts.
   - Запусти проверку типов (npx tsc --noEmit в папке worker), чтобы убедиться в отсутствии ошибок.

2. Коллектор (collector/src/collector.ts):
   - Внимательно просмотри обработчик messageCreate.
   - Убедись, что кулдаун сообщений (45 секунд) блокирует ИСКЛЮЧИТЕЛЬНО начисление XP (xpToAdd = 0).
   - Счётчик общего количества сообщений пользователя (users.messages_count = messages_count + 1) и суточная статистика (user_daily_activity.messages_count = messages_count + 1) ДОЛЖНЫ инкрементироваться на абсолютно каждое валидное сообщение от пользователя.
   - Убедись, что нет никаких условий или досрочных return, пропускающих блок upsert в user_daily_activity при активном кулдауне.
   - Запусти сборку коллектора (npm run build в папке collector), исправь любые ошибки, если они возникнут.

3. Все необходимые изменения закоммить в git.
