Задача: починить два известных бага биржи и составить отчёт о расхождениях гайда и кода. Экономику и формулы НЕ меняй.

1. Несовпадение имён опций. По гайду collector.ts регистрирует опцию /invest и /divest как `company`, а обработчики читают опцию `ticker` (получают пустую строку и отвечают «Компания с таким тикером не найдена»).
   - Найди ВСЕ места регистрации slash-команд (grep: `commands.create`, `commands.set`, `register`, `deploy`).
   - Сверь имена опций с тем, что читают handleInvest / handleDivest / handlePortfolio / handleStocks.
   - Приведи к единому имени `ticker` везде (регистрация + обработчики), описание опции «Тикер компании». Если опция уже совпадает, ничего не меняй и так и напиши.

2. Инверсия правила контрольного пакета в divest.ts. Сейчас в UPDATE company_shares условие `(shares_count - ? >= 51 OR ? = 0)` получает параметр `isOwner ? 0 : 1`, из-за чего ограничение действует на не-владельцев, а владелец может продать всё. Задуманное поведение: у ВЛАДЕЛЬЦА после продажи должно оставаться минимум 51 акция, у остальных ограничения нет.
   - Исправь так, чтобы параметр был `isOwner ? 1 : 0`, либо перепиши условие явно и читаемо. Введи именованную переменную (например `enforceControlStake`).
   - Сохрани атомарность (проверка внутри самого UPDATE) и текст ошибки; проверь, что текст соответствует реальному правилу.
   - Не трогай остальное в запросе.

3. Отчёт (только отчёт, без правок): в конце ответа перечисли расхождения «гайд говорит / код делает» по пунктам:
   - какие точные формулы у invest/divest сейчас и чем они отличаются от описанных в EXCHANGE_GUIDE.md;
   - какой libsql-клиент импортируется в worker и в collector, и используется ли где-то db.transaction();
   - как защищён db.batch в invest.ts и divest.ts от гонок (какие именно условия в WHERE);
   - как collector отправляет сообщения в каналы (какой клиент/REST), и есть ли в проекте отрисовка изображений (satori/resvg/canvas);
   - как устроено хранение служебных значений по гильдии (маркеры вроде last_season_reset).

Критерий готовности: проект компилируется (typecheck), в diff только эти два исправления, в ответе есть отчёт из п.3.
