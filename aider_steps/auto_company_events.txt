Упрости механику кризисов в автоматические события компаний (Company Events) и обнови описания команд:

1. В collector/src/crises.ts:
   - Полностью убери интерактивный выбор кнопками, ожидание дедлайнов и 24-часовые таймауты.
   - Сделай события мгновенными (автоматическими):
     * При срабатывании тикера выбирается компания и генерируется событие:
       - 50% шанс ПОЛОЖИТЕЛЬНОГО события (Госзаказ, Прорыв технологий, Успешный маркетинг, Рекордный квартал, Инвестиционный бум):
         Казна компании `treasury` увеличивается на 10% - 25% (минимум +150 🪙).
       - 50% шанс ОТРИЦАТЕЛЬНОГО события (Налоговая проверка, Сбой оборудования, Утечка данных, Судебная издержка, Скандал в прессе):
         Казна компании `treasury` уменьшается на 5% - 20% (но не ниже 50 🪙).
     * Изменение казны применяется МГНОВЕННО через UPDATE companies SET treasury = ...
     * Записывай событие в company_crises сразу со статусом 'resolved' (для истории).
     * Добавляй запись в company_nav_history с reason 'crisis'.
     * Ставь в market_events сочный анонс для ленты Discord:
       Для положительного: 🎉 **Событие компании:** У **${name}** (`${ticker}`) успешное событие: ${title}! Казна выросла на **+${delta} 🪙**!
       Для отрицательного: 📉 **Инцидент:** Компания **${name}** (`${ticker}`) понесла убытки: ${title}. Казна сократилась на **-${delta} 🪙**.
   - Добавь автоматический сброс старых зависших кризисов:
     UPDATE company_crises SET status = 'resolved' WHERE status = 'pending'

2. В worker/src/commands/stocks.ts:
   - Убери запрос активных кризисов из company_crises со статусом 'pending' и вывод строки `⚠️ Кризис: осталось...`, так как события теперь мгновенные.

3. В worker/src/worker.ts:
   - Убери или заглуши маршрут кнопок `crisis:` (так как кнопок больше нет).

4. В collector/src/collector.ts:
   - Исправь описания всех команд (/map, /plot, /build, /upgrade, /auction, /exchange-setup, /exchange-top) на чистый русский текст в UTF-8.
   - В цикле регистрации команд для гильдий сделай:
     if (existingCmd) {
       await existingCmd.edit(cmd);
     } else {
       await guild.commands.create(cmd);
     }
     (Чтобы при старте бота существующие команды обновили свои описания на чистый русский язык).

5. Проверь типы TypeScript (cd collector && npx tsc --noEmit, cd worker && npx tsc --noEmit) и сделай git commit.
